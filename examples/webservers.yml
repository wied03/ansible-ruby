---
# This is a generated YAML file by ansible-ruby, DO NOT EDIT
- hosts: host1
  name: webserver install
  tasks:
  - name: install nginx
    yum:
      name: nginx
      state: absent
  - name: EC2 load balancer
    ec2_elb_lb:
      state: present
      name: lb_01
      security_group_ids: id1,id2
      listeners:
        '22': tcp