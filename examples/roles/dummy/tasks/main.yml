---
# This is a generated YAML file by ansible-ruby, DO NOT EDIT
- name: say hello
  shell: ls {{ item }} || true
  args:
    chdir: "/tmp"
  with_items:
  - a
  - b
  - c
  register: result_1
  changed_when: "'No such filesss' in result_1.stderr"
  notify:
  - reboot
- name: middle
  debug:
    msg: foo
- name: and goodbye
  command: ls {{ item.key }} {{ item.value.foobar }}
  args:
    chdir: "/tmp"
  with_dict:
    cmd1:
      foobar: '123'
    cmd2:
      foobar: '456'
  register: result_2
  changed_when: "'No such filesss' in result_2.stderr"