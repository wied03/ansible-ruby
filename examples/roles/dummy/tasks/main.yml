---
# This is a generated YAML file by ansible-ruby, DO NOT EDIT
- name: say hello
  shell: ls {{ item }} || true
  args:
    chdir: "/tmp"
  with_items:
  - a
  - b
  - c
  register: result_1
  failed_when: "'No such filesss' in result_1.stderr"
- name: and goodbye
  command: ls {{ item.key }} {{ item.value.foobar }}
  args:
    chdir: "/tmp"
  with_dict:
    cmd1:
      foobar: '123'